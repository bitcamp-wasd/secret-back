spring:
  application:
    name: video
  config:
    import: optional:configserver:http://10.0.8.8:8082
  profiles:
    active: ${setting.profile.type}
  devtools:
    restart:
      enabled: true

server:
  port: 8083


