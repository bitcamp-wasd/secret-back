#config:
#  #    config server host
#  import: optional:configserver:http://localhost:8082
#
#profiles:
#  active: ${profile.type}

spring:
  application:
    name: video
  config:
    import: optional:configserver:http://localhost:8082
  profiles:
    active: ${setting.profile.type}

server:
  port: 8083


